{
  "name": "gps",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently --names \"BACKEND,FRONTEND\" -c \"cyan,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
