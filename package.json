{
  "name": "vehicleMS",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently --names \"VEHICLE_MS_BACKEND,VEHICLE_MS_FRONTEND\" -c \"cyan,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
